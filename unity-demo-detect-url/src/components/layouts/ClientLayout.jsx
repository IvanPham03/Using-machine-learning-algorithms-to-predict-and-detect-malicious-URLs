import React from 'react'
const ClientLayout = ({ children }) => {
    // console.log(children);

    return (
        <div>
            {children}
        </div>
    )
}

export default ClientLayout
